# 游乡记任务记忆文档
更新时间：2026-03-25  
仓库路径：`D:\wzb\codex`  
当前协作基线：远程仓库 `origin/master`

## 任务目标
- 把“游乡记”从课程展示型页面持续整理成可协同开发的旅游平台原型。
- 平台默认使用数据库数据，不再依赖本地 demo 景点。
- 优先完善首页、地图、景点详情、智能规划、登录与后台管理这些核心链路。
- 保持中文化、可运行、可构建，并且页面表达更像真实旅游产品而不是作业展示。

## 2026-03-24 已完成
### 数据与图片
- 全国旅游画像数据已整合进数据库，平台景点库大幅扩充。
- 针对缺图景点做了多轮补图，绝大多数全国画像景点现在已有图片。
- 首页展示图片来源改为优先使用西安本地静态图和稳定可访问的图片资源，避免首页出现灰图、透明图或无关图片。

### 首页与界面
- 首页首屏、统计卡、专题区、目的地推荐区做了多轮重构。
- 首页统计内容改为动态数据：
  - 已注册用户数
  - 当日浏览数
  - 已录入景点数
- 首页图片专题区改成更偏编辑感和旅游平台感的表达，不再出现“能力展示”式文案。
- 首页管理员入口已补到页脚，后台页已加管理员权限校验。

### 登录与后台
- 修复退出登录后仍残留用户名/管理员状态的问题。
- 后台页已接入管理员权限保护，不再允许游客直接进入后台。
- 登录、注册、后台入口相关文案与交互做过一轮整理。

### 地图与景点
- 地图页已恢复可用，景点详情与地图联动正常。
- 首页和地图页都改为优先使用更稳定的本地图片和已整理景点数据。

## 2026-03-25 本轮更新
### 编码与文案清理
- 统一复查 `TASK_MEMORY.md` 与核心页面文件的 UTF-8 编码状态，避免任务交接文档再次出现乱码。
- 把首页、地图页、景点库、智能规划页里残留的英文系统标签统一收成中文，降低“半成品”感并避免与乱码混淆。

### 登录入口拆分
- 普通用户登录页保留为 `/login`
- 管理员登录页新增为 `/admin/login`
- 管理员后台入口与普通用户入口正式拆开，不再共用一个登录页
- 登录接口新增角色校验：
  - 用户登录页默认只允许普通用户登录
  - 管理员登录页默认只允许管理员登录
  - 管理员误走用户登录页会收到明确提示
  - 普通用户误走管理员登录页也会被拦截

### 地图展示规则调整
- 乡村地图现在按以下规则显示点位：
  - 西安地区景点正常展示
  - 西安以外地区只展示 5A 景点
- 地图弹窗优先展示景点地址；没有地址时再回退到城市 / 区县信息
- 地图页文案已同步更新，向用户说明这个筛选规则

### 任务记忆文档
- 旧版文档存在乱码，已整体改写为 UTF-8 可读版本
- 已补入 2026-03-24 的工作内容和本轮最新改动

## 当前关键决策
### 1. 运行数据以数据库为主
原因：
- 后续导入、补图、地图、搜索、规划都必须依赖数据库
- demo 数据不适合继续承担平台主数据源

### 2. 首页图片优先使用西安本地静态资源
原因：
- 远程图片不稳定，容易出现灰图、热链失败、透明 PNG 等问题
- 首页需要优先稳定，而不是追求“数据源完全自动”

### 3. 普通用户登录与管理员登录必须拆分
原因：
- 用户和后台管理是两种完全不同的任务场景
- 共用同一登录页容易误进、误操作，也不利于后续权限管理

### 4. 地图默认收敛外地景点
原因：
- 全国数据导入后，地图点位过多会影响可读性与性能
- 当前平台的核心仍然是“西安周边周末出行”，外地景点只保留 5A 作为补充参考更合理

## 当前关键文件
- `app/page.tsx`
  首页视觉、专题区、路线专题区
- `app/layout.tsx`
  全局导航、页脚、管理员入口
- `app/login/page.tsx`
  普通用户登录页
- `app/admin/login/page.tsx`
  管理员登录页
- `components/auth-form.tsx`
  登录/注册表单，已支持用户/管理员入口区分
- `app/api/auth/login/route.ts`
  登录接口，已支持角色校验
- `app/map/page.tsx`
  地图页面
- `components/map-client.tsx`
  地图弹窗与点位展示
- `lib/repository.ts`
  地图数据、首页数据、景点查询等仓储逻辑

## 待解决问题
- 首页专题区虽然已经重构多轮，但视觉节奏仍在继续调，尤其是中段专题区的版式与留白。
- 局部页面仍有少量旧文案或乱码遗留，需要继续清理。
- 全国数据量上来以后，智能规划链路后面仍然需要进一步做候选过滤与性能优化。
- 外地景点虽然已补很多图，但仍有少量景点图片质量和一致性不足。

## 特有约束
- 平台表达必须像正常旅游平台，不要再出现“核心能力展示”“产品路演式说明”。
- 后续新数据必须优先走导入脚本和数据库，不要回退到本地 demo。
- 仓库是协作仓库，不能随意回滚不属于当前任务的修改。
- 首页和地图页优先保证稳定和可读性，其次再追求更复杂的视觉实验。

## 接手建议
1. 先运行：
   - `npm run build`
   - `npm run start`
2. 优先检查：
   - `/`
   - `/login`
   - `/admin/login`
   - `/admin`
   - `/map`
3. 如果继续做首页 UI，重点看 `app/page.tsx`
4. 如果继续做权限或登录，重点看：
   - `components/auth-form.tsx`
   - `app/api/auth/login/route.ts`
   - `lib/auth.ts`
5. 如果继续做地图过滤或性能，重点看：
   - `app/map/page.tsx`
   - `components/map-client.tsx`
   - `lib/repository.ts`
